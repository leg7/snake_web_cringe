lancer le script `./run.sh` pour lancer l'appli

`sqlite3 app/database.db` pour consulter la base fictive

Login exemple:

nick: leonard
pass: leonardo

Page accessible a [app](http://localhost:8080/index)
